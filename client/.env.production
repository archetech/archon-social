# Production environment — standalone archon.social deployment
# The React client hits '/api' (relative), which nginx proxies to the
# Herald server on 127.0.0.1:4230. See nginx/archon-social.conf.
VITE_API_URL=/api
